import './Header.scss';

const Header = () => {
    return (
        <header className="notify">
            <input type="checkbox" />
            <p>
                aa
            </p>
        </header>
    );
};

export default Header;